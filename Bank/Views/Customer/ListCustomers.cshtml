@model Bank.ViewModels.ListCustomersViewModel


@{
    ViewData["Title"] = "ListCustomers";
}

 <table class="table">
     <thead>   
         <tr>
            <th scope="col">Kundnummer</th>
            <th scope="col">Personnummer</th>
            <th scope="col">Namn</th>
            <th scope="col">Address</th>
            <th scope="col">Stad</th>
         </tr>
     </thead>
     <tbody>
         @foreach(var customer in Model.Customers)
         {
             <tr>
                 <th scope="row">@customer.CustomerId</th>
                 <td>@customer.NationalId</td>
                 <td>@customer.Givenname @customer.Surname</td>
                 <td>@customer.Streetaddress</td>
                 <td>@customer.City</td>
                 <td><a asp-controller="Customer" asp-action="searchCustomer" asp-route-id="@customer.CustomerId">Välj</a></td>
             </tr>
         }
     </tbody>
 </table> 

    <partial name="_PagerPartial" model="Model.PagingViewModel">
           
      

